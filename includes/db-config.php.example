<?php
/**
 * Database Configuration
 * 
 * INSTRUCTIONS:
 * 1. Copy this file to db-config.php
 * 2. Fill in your database credentials from Hostinger
 * 3. Never commit db-config.php to version control!
 * 
 * You can find your database credentials in Hostinger:
 * - Go to hPanel → Databases → MySQL Databases
 * - Or check your database details in phpMyAdmin
 */

// Database host (use 127.0.0.1:3306 if shown in phpMyAdmin, or just 127.0.0.1)
// On Hostinger, this is usually 'localhost' or '127.0.0.1'
define('DB_HOST', '127.0.0.1');
define('DB_PORT', '3306'); // Default MySQL port (usually 3306)

// Database name (the database you created)
define('DB_NAME', 'your_database_name');

// Database username
define('DB_USER', 'your_database_user');

// Database password
define('DB_PASS', 'your_database_password');

