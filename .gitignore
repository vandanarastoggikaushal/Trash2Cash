# Dependencies
node_modules/
package-lock.json

# Build output (keep the compiled CSS file, but ignore source maps if any)
*.map

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Data files (runtime user submissions)
data/*.json
!data/.gitkeep

# Database configuration (contains credentials)
includes/db-config.php